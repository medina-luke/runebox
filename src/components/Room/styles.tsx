import styled from "styled-components";

export const Container = styled.section`
    margin: 0 auto;
    padding: 16px;
`

export const RuneCount = styled.span`
    color: var(--special-color);
`

export const ActionContainer = styled.section`
    display: flex;
    flex-flow: column nowrap;
    align-items: center;
    gap: 12px;
`